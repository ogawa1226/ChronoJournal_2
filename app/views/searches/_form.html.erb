<%= form_with url: searches_path, local: true, method: :get do |f| %>
  <%= f.text_field :word %>
  <%= f.select :range, options_for_select([['スケジュール'], ['振り返り']]) %>
  <%= f.submit "検索", class: "btn-sm btn-primary" %>
<% end %>